# ADR-050 MVP IMPLEMENTATION FOUNDATION

## Decision

The project will begin MVP implementation inside the existing repository using a monorepo-style structure.

## Reason

The architecture workbook and implementation code should remain connected while development begins.

## Consequence

Application code will live in apps, shared packages will live in packages, infrastructure will live in infrastructure, and helper scripts will live in scripts.

## Status

Accepted
